# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

The rules are not duplicated here. [AGENTS.md](AGENTS.md) is the single source of truth for this repository — every command, invariant, and convention lives there, in tool-agnostic form with the reasoning behind each one. Edit that file; never copy its content back into this one.

The import below pulls it into context automatically, so nothing needs to be kept in sync:

@AGENTS.md
